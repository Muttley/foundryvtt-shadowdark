<div class="item-grid">
	<h3>{{localize 'SHADOWDARK.boon.type.label'}}</h3>
	<select name="system.boonType">
		{{selectOptions
			config.BOON_TYPES
			selected=system.boonType
			localize=false
		}}
	</select>
</div>

