<form class="{{cssClass}}" autocomplete="off">
	<header class="item-header">
		<img
			src="{{item.img}}"
			data-edit="img"
			title="{{item.name}}"
			height="64"
			width="64"
		/>
		<h1>
			<input
				class="item-name"
				name="name"
				type="text"
				value="{{item.name}}"
				placeholder="{{localize 'Name'}}"
			/>
		</h1>
	</header>
	<section class="item-body">
		{{#ifEq item.type "Weapon"}}
			{{> items/partials/weapon.hbs }}
		{{/ifEq}}

		{{#ifEq item.type "Armor"}}
			{{> items/partials/armor.hbs }}
		{{/ifEq}}

		{{> items/partials/description.hbs }}

		<div>
			<div class="item-sidebar">

				{{#ifEq item.type "Talent"}}
					{{> items/partials/talent.hbs }}
				{{/ifEq}}

				{{#if hasCost}}
					{{> items/partials/cost.hbs }}
				{{/if}}

				{{#if usesSlots}}
					{{!-- Gems have non-configurable slot settings --}}
					{{#ifNeq item.type "Gem"}}
						{{> items/partials/slots.hbs }}
					{{else}}
						<div></div>
					{{/ifNeq}}

					{{#if item.system.canBeEquipped}}
						{{> items/partials/equipped.hbs }}
					{{/if}}
				{{/if}}

				{{#ifEq item.type "Basic"}}
					{{> items/partials/treasure.hbs }}
				{{/ifEq}}

			</div>
		</div>
	</section>
</form>
