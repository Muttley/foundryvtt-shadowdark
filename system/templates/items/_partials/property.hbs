<div class="item-grid">
	<h3>{{localize 'SHADOWDARK.property.type.title'}}</h3>
	<select name="system.itemType">
		<option value="">&mdash;</option>
		{{selectOptions
			config.PROPERTY_TYPES
			selected=system.itemType
			localize=false
		}}
	</select>
</div>
